=== Task 1-3: Backend Verification ===
Date: 2026-06-13 19:20
Status: BLOCKED — Backend not running

Evidence:
  $ curl -s -o /dev/null -w "%{http_code}" http://localhost:5000/api/health
  → 000 BACKEND_NOT_RESPONDING (timeout after 10s)

Docker status:
  Container: wishlist-db (postgres:16-alpine) — Up 4 hours, healthy
  Backend container (wishlist-api or similar): NOT running

Scenarios blocked (require POST /api/auth/*, /api/events/*, /api/guests/*):
  1. Register a user  — POST /api/auth/register
  2. Login           — POST /api/auth/login
  3. Create event    — POST /api/events (requires JWT)
  4. Add guest       — POST /api/events/{id}/guests (requires JWT)
  5. Update RSVP     — POST /api/guests/{token}/rsvp
  6. Get invite page — GET /api/guests/by-token/{token}
  7. curl-based API verification of any endpoint

Resolution needed:
  Start backend via `docker compose up -d` or `dotnet run` in backend/WishlistApp.API/
  Then re-run curl verification against each endpoint.

Backend source reviewed:
  - Controllers/Controllers.cs: All endpoints exist and match frontend expectations
  - GuestPublicDto includes guestCount in RSVP notification (line 196)
  - Guest model includes GuestCount field
